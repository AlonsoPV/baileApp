# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local
config/local.env
google-service-account.json

# generic build artifacts
build/

# typescript
*.tsbuildinfo

# CocoaPods (iOS)
# - Never commit Pods/ (generated)
# - Always commit Podfile.lock (pins native deps)
ios/Pods/
!ios/Podfile.lock

# iOS AppIcon sizes are generated in CI (`ci_scripts/ci_pre_xcodebuild.sh`)
# Keep the committed 1024 source/opaque files, ignore generated sizes.
ios/DondeBailarMX/Images.xcassets/AppIcon.appiconset/AppIcon-[0-9]*@*.png
ios/DondeBailarMX/Images.xcassets/AppIcon.appiconset/AppIcon-83.5@*.png

# generated native folders
# NOTE: We commit `ios/` because Xcode Cloud needs a workspace present at clone time.
# If you switch back to fully managed workflow (EAS-only), you can re-add `/ios`.
# /ios
/android

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
